name: "alzheimers_analytics"
version: "1.0.0"
config-version: 2

profile: "alzheimers_analytics"

model-paths:
  - "models"

test-paths:
  - "tests"

seed-paths:
  - "seeds"

clean-targets:
  - "target"
  - "dbt_packages"

models:
  alzheimers_analytics:
    staging:
      +materialized: view


    marts:
      +materialized: table
